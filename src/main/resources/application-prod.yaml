server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/HRM_User_Service_Dev
    username: postgres
    password: Rajat@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    org.springframework.security: DEBUG